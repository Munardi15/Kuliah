@model IEnumerable<kuliah.Models.Marakuliah>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kode_MK)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nama_MK)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sks)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Kode_MK)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nama_MK)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sks)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Kode_MK">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Kode_MK">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Kode_MK">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
