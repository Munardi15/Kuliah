@model IEnumerable<kuliah.Models.Dosen>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nama_Dosen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nip)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nama_Dosen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Nip">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Nip">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Nip">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
